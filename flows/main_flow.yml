id: main_flow
namespace: shopzada.flows

tasks:
  - id: dbt
    type: io.kestra.plugin.core.flow.WorkingDirectory
    tasks:
      - id: run_clone_repository
        type: io.kestra.plugin.core.flow.Subflow
        namespace: shopzada.flows
        flowId: clone_repository

      - id: run_create_db
        type: io.kestra.plugin.core.flow.Subflow
        namespace: shopzada.flows
        flowId: create_db
